if filters.get("psid"):
    # Splits the column string at the '-' and compares only the first segment
    where_clause += " AND SPLIT_PART(psid, '-', 1) = %s"
    params.append(str(filters["psid"]).strip())
