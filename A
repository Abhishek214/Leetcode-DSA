# pvt.py

from your_module import AzureDocOCRProcessor


def main():
    required_methods = [
        "convert_polygon_bbox_pixel",
        "process_pages",
    ]

    for method in required_methods:
        assert hasattr(AzureDocOCRProcessor, method), (
            f"Missing method: {method}"
        )

    print("✅ PVT PASSED")
    print("All optimized methods are available")


if __name__ == "__main__":
    main()
