Extract the address from the provided document and format it as a single, combined string. Return a JSON object with exactly one key: "Business Address".

TARGET SECTION INSTRUCTION (CRITICAL):
1. ONLY extract the address from the section explicitly labeled "Contact Information (Must be Completed)" / "Business Address".
2. Strictly ignore any other address fields in the document, such as "Correspondence Address" or other sections.

CRITICAL VISUAL INSTRUCTIONS:
1. The "Room/Flat" field consists of empty boxes separated by vertical lines. 
2. DO NOT interpret these vertical printed grid lines as numbers. There is no typed or handwritten text in this field.
3. KNOWN ERROR: You may incorrectly detect the number "31" or "1" due to the grid lines. If you see "31" or "1" in the Room/Flat field, ignore it entirely and treat the field as blank.

FORMATTING RULES:
- Combine all valid extracted components (Floor, Block, Building, Street, District, Country) logically, separated by commas.
- Omit any fields that are blank or empty in the image. Do NOT include words like "N/A" or "null" in the final combined address string.

Example of Expected Output format:
{"Business Address": "Floor 8, Block I, NEW WORLD TOWER, 18 QUEEN'S ROAD CENTRAL, CENTRAL"}
